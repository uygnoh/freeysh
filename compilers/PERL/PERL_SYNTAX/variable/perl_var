### ======================================================== perl正则变量
%匹配成功的子串之前的内容
$`
$PREMATCH

%成功模式匹配的字符串	
$&
$MATCH
	
%匹配成功的子串之后的内容
$'
$POSTMATCH

%与上个正则表达式搜索格式匹配的最后一个括号。
%例如: /Version: (.*)|Revision: (.*)/ && ($rev = $+);
$+
$LAST_PAREN_MATCH	














